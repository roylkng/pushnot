<!DOCTYPE html>
<html>

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>CONNECTO | Forgot Password</title>

    <link href="css/bootstrap.min.css" rel="stylesheet">
    <link href="font-awesome/css/font-awesome.css" rel="stylesheet">

    <link href="css/animate.css" rel="stylesheet">
    <link href="css/style.css" rel="stylesheet">

</head>

<body class="gray-bg">

    <div class="middle-box text-center loginscreen  animated fadeInDown">
        <div>
            <div>
                <h2 class="logo-name" style="font-size: 63px;">CONNECTO</h2>
            </div>
            {{#if flag1}}
                <p class="alert alert-danger">{{message1}}</p>
            {{/if}}
            {{#if flag2}}
                <p class="alert alert-info">{{message2}}</p>
            {{/if}}
            {{#if flag3}}
                <p class="alert alert-warning">{{message3}}</p>
            {{/if}}
            <form class="m-t" role="form" action="/forgot-password" method="post">
                <div class="form-group">
                    <input type="email" class="form-control" placeholder="Registered email address of your account" name="email" required="">
                </div>
                <button type="submit" class="btn btn-primary block full-width m-b">Send link for password reset</button>
                <a href="/login">Back to login page</a>
            </form>
        </div>
    </div>

    <!-- Mainly scripts -->
    <script src="js/jquery-2.1.1.js"></script>
    <script src="js/bootstrap.min.js"></script>
    {{>common_js}}
</body>

</html>
